from enum import Enum

class TurnTypeEnum(Enum):
    CHALLENGE = "challenge_type"
    GROUP_CHALLENGE = "group_challenge_type"
    SECRET_MISSION = "secret_mission_type"
    TARGET_CHALLENGE = "target_challenge_type"